<tr<% if line_item == @current_item %> id="current_item"<% end %>>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.title %></td>
  <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
  <!-- <td><%= button_to 'Delete this title', line_item, method: :delete, confirm: 'Are you sure?' %></td> -->
</tr>